# Project Template

这是一个实验项目模板。

## 使用方法

1. 复制此目录到新项目
2. 修改 `config.yaml` 配置
3. 运行 `python run_experiment.py`

## 文件结构

```
your_project/
├── config.yaml           # 实验配置
├── run_experiment.py     # 实验入口
├── custom_components.py  # 自定义组件（可选）
└── README.md
```
